import { ArrowUpRight, BriefcaseBusiness, Code2 } from 'lucide-react'; import { links } from '../data/portfolio'; import Reveal from './Reveal'; import SectionHeading from './SectionHeading';
const profiles = [[Code2, 'GitHub', 'Source code and project repositories', links.github], [BriefcaseBusiness, 'LinkedIn', 'Professional profile and connections', links.linkedin], [null, 'LeetCode', '500+ data structures and algorithms problems', links.leetcode]];
export default function CodingProfiles() { return <section className="section shell" aria-labelledby="profiles-title"><SectionHeading eyebrow="07 / Profiles" title="Code, practice, and professional context." /><div className="profiles" id="profiles-title">{profiles.map(([Icon, title, text, href], index) => <Reveal as="a" className="profile-card" delay={index * .07} key={title} href={href} target="_blank" rel="noreferrer"><span className="profile-card__icon">{Icon ? <Icon /> : 'LC'}</span><div><h3>{title}</h3><p>{text}</p></div><ArrowUpRight /></Reveal>)}</div></section>; }
